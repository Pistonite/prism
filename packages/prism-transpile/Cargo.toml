[package]
name = "prism-transpile"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
cu = { workspace = true, features = ["fs"] }
swc = "56.0.0"
swc_common = "19.0.0"
swc_ecma_ast = "21.0.0"
swc_ecma_parser = "35.0.0"
swc_ecma_transforms_typescript = "42.0.0"

# # Additional feature required by wasm
# [dependencies.getrandom]
# version = "0.4.2"
# optional = true

[features]
wasm = [
    # "dep:getrandom",
    # "getrandom/wasm_js",
]
